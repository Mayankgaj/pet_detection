AUGMENTATION: True # If dataset is limited then make it true and increase dataset no. (tilt, rotate) 
IMAGE_SIZE: [224, 224, 3] # as per VGG 16 model
BATCH_SIZE: 16
INCLUDE_TOP: False
EPOCHS: 1
CLASSES: 2 # Dog & Cat
WEIGHTS: imagenet
LEARNING_RATE: 0.01
